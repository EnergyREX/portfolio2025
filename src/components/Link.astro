---
interface Props {
  url: string;
}

const { url } = Astro.props;

---

<a class="p-2 px-3 bg-neutral-400 dark:bg-neutral-700 hover:scale-105 duration-200 hover:bg-neutral-800 rounded-md" 
href={ url }><slot /></a>