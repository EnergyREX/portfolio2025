
const SUPPORTED_LANG = ['es', 'en']

// For status (if available or not)
const STATUS = true;

const AVAILABILITY_MSG = {
  es: {
    not: 'Ocupado',
    yes: 'Disponible'
  },
  en: {
    not: 'Unavailable',
    yes: 'Available'
  }
}


export {SUPPORTED_LANG, AVAILABILITY_MSG, STATUS}